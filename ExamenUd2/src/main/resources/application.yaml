spring:
  jpa.hibernate.ddl-auto: validate
  datasource:
    url: jdbc:mysql://localhost:3306/empleados?createDatabaseIfNotExist=true
    username: root
    password: toor
  hikari:
    connection_timeout: 60000
    maximum-pool-size: 5
server:
  port: 8081
